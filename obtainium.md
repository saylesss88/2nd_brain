# Obtainium

Once downloaded and installed, open `Obtainium -> + Add app`.

Search for the app you want to install and visit their github/gitlab page. Copy
the url and paste it into `Add Source URL`, you can leave the default options
and install the app.

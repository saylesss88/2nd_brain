Create a symbolic link to the `~/bin` directory for helix and neovim:

```bash
ln -s ~/.local/share/flatpak/exports/bin/com.helix_editor.Helix ~/bin/hx
ln -s ~/.local/share/flatpak/exports/bin/io.neovim.nvim  ~/bin/nvim
```
